# Role: base

## Назначение

Роль base выполняет базовую настройку операционной системы и формирует минимальный унифицированный baseline рабочей станции.

Роль предназначена для начальной подготовки системы перед применением последующих ролей (users, security, docker, dev).

---

## Что делает роль

### Для Ubuntu

* обновляет пакетный кэш;
* устанавливает базовые системные пакеты;
* настраивает timezone;
* настраивает locale.

### Для Astra Linux Orel

* обновляет пакетный кэш;
* устанавливает базовые системные пакеты;
* настраивает timezone;
* генерирует и применяет locale;
* использует отдельную реализацию через raw для совместимости с платформой.

---

## Что не входит в роль

Роль base не выполняет:

* создание пользователей;
* настройку SSH-доступа;
* hardening системы;
* установку Docker;
* установку инструментов разработки;
* настройку графической среды.

Эти задачи вынесены в отдельные роли.

---

## Поддерживаемые платформы

* Ubuntu 24.04
* Astra Linux Orel

---

## Основные переменные

Определяются в defaults/main.yml.

### base_packages

Список базовых пакетов, устанавливаемых на систему.

Пример:

base_packages:
  - curl
  - wget
  - git
  - vim
  - ca-certificates
  - unzip

### base_timezone

Часовой пояс системы.

Пример:

base_timezone: "Europe/Moscow"

### base_locale

Системная locale.

Пример:

base_locale: "en_US.UTF-8"

---

## Структура задач

tasks/
├── main.yml
├── ubuntu.yml
├── astra.yml
└── common.yml

* main.yml — маршрутизация выполнения;
* ubuntu.yml — Ubuntu-специфичная логика;
* astra.yml — Astra-специфичная логика;
* common.yml — общие задачи (при необходимости).

---

## Пример использования

- name: Apply base configuration
  hosts: workstations
  become: true

  roles:
    - base

---

## Особенности реализации

На Astra Linux стандартный модуль apt используется ограниченно из-за платформенных особенностей Python-интерпретатора. Поэтому часть базовой конфигурации реализована через raw.

---

## Ограничения текущей версии

* состав пакетов задаётся без жёсткой фиксации версий;
* hostname не настраивается;
* дополнительные системные политики не применяются.
