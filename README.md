# Automated Corporate Workstation Setup (Ansible)

## 📌 Описание проекта

Проект представляет собой систему автоматизированной настройки корпоративных рабочих станций разработчиков с использованием Ansible.

Цель проекта — обеспечить воспроизводимую, централизованную и безопасную настройку рабочих машин, минимизируя ручную конфигурацию и снижая вероятность ошибок.

Поддерживаемые операционные системы:

* Ubuntu 24.04
* Astra Linux Orel

---

## 🎯 Цели проекта

* автоматизация настройки рабочих станций;
* унификация окружения разработчиков;
* централизованное управление доступом;
* повышение уровня корпоративной безопасности;
* снижение ручного администрирования;
* поддержка мультиплатформенной инфраструктуры.

---

## 🏗 Архитектура проекта

Проект построен на основе ролей Ansible:

* base — базовая настройка системы
* users — управление учетными записями и доступом
* security — базовые политики безопасности (в разработке)
* docker — установка Docker (в разработке)
* dev — инструменты разработки (в разработке)

---

## ⚙️ Текущий статус проекта

### ✅ Реализовано

#### Инфраструктура

* развернут тестовый стенд:

  * управляющая машина с Ansible;
  * отдельный хост с виртуальными машинами;
* настроен доступ по SSH:

  * авторизация по ключам;
  * конфигурация через ~/.ssh/config.

#### Совместимость Astra Linux

* реализована установка Python 3.9 вручную;
* настроен ansible_python_interpreter;
* обеспечена работоспособность Ansible на Astra Linux.

#### Роль base

* обновление пакетного кэша;
* установка базовых пакетов;
* настройка timezone;
* настройка locale;
* реализована отдельная логика для Astra через raw.

#### Роль users

* создание и управление учетными записями;
* назначение shell и групп;
* установка SSH-ключей;
* управление sudo через /etc/sudoers.d;
* поддержка state: present и state: absent;
* конфигурация пользователей вынесена в group_vars;
* реализована мультиплатформенная поддержка (Ubuntu / Astra).

---

## 🔐 Особенности безопасности

* использование SSH-ключей вместо паролей;
* централизованное управление доступом;
* безопасная настройка sudo через /etc/sudoers.d;
* валидация sudoers через visudo;
* минимизация ручного вмешательства в систему.

---

## ⚠️ Особенности Astra Linux

В Astra Linux используется отдельная логика установки пакетов:

* стандартный модуль apt не применяется;
* используется raw из-за несовместимости python3-apt с кастомным Python;
* реализован bootstrap-этап подготовки системы.

---

## 🚀 Запуск

### Базовая настройка системы

ansible-playbook playbooks/base.yml

### Управление пользователями

ansible-playbook playbooks/users.yml

---

## 📁 Структура проекта

.
├── ansible.cfg
├── inventories/
│   ├── inventory.ini
│   └── group_vars/
│       └── workstations.yml
├── playbooks/
│   ├── base.yml
│   └── users.yml
├── roles/
│   ├── base/
│   └── users/
├── scripts/
│   └── install-python.sh
└── docs/

---

## 🚧 В разработке

* роль security (SSH hardening, политики доступа);
* автоматизация установки Docker;
* настройка среды разработки (Python + pipx);
* интеграция инструментов разработки;
* улучшение управления пользователями (users v2);
* улучшение хранения SSH-ключей.

---

## 📈 Планы развития

* реализация полноценной корпоративной политики безопасности;
* повышение идемпотентности для Astra Linux;
* поддержка нескольких типов пользователей;
* расширение мультиплатформенной поддержки;
* подготовка проекта к реальному использованию в компании.

---

## 📄 Примечание

Проект разрабатывается в рамках дипломной работы и отражает практическое решение задач автоматизации и стандартизации рабочих станций разработчиков.
